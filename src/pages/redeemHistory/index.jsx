import { Layout } from "../../layout/layout";
import { RedeemHistory } from "../../components/redeemHistory";

export const RedeemHistoryPage = () => {
  return (
    <Layout>
      <RedeemHistory />
    </Layout>
  );
};

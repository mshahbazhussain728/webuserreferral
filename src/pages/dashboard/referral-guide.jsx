import { Layout } from "../../layout/layout";
import { ReferralGuide } from "../../components/dashboard/referral-guide";

export const ReferralGuidePage = () => {
  return (
    <Layout>
      <ReferralGuide />
    </Layout>
  );
};
